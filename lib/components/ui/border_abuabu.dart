part of 'ui.dart';

class BorderAbuAbu extends StatelessWidget {
  const BorderAbuAbu({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 8,
      color: Colors.grey[200],
    );
  }
}
